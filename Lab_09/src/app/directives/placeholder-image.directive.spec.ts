import { PlaceholderImageDirective } from './placeholder-image.directive';

describe('PlaceholderImageDirective', () => {
  it('should create an instance', () => {
    const directive = new PlaceholderImageDirective();
    expect(directive).toBeTruthy();
  });
});
